# Completed production media now cold-archived in the verified assets.json source package.
# Final user-facing MP4 remains in /home/user; tracked edit scripts/provenance remain local.
# Before a future revision, restore missing files using tools/restore-hyperframes-project.py --apply.
